# 🔍 Інструкція з тестування управління продуктами

## Крок 1: Відкрийте консоль браузера

1. Відкрийте http://localhost:3000/admin
2. Натисніть **F12** для відкриття DevTools
3. Перейдіть на вкладку **Console**

## Крок 2: Перейдіть на вкладку "Продукти"

1. Натисніть на вкладку **🎯 Продукти**
2. Ви повинні побачити список існуючих продуктів

## Крок 3: Тестування створення продукту

1. Натисніть кнопку **➕ Створити продукт**
2. Заповніть форму:
   ```
   Назва: Тестовий продукт
   Категорія: Криптовалюта
   Мінімальна інвестиція: 1000
   Прибутковість: 20
   Термін: 12
   Ризик: Високий
   Опис: Це тестовий продукт
   Активний: ✓
   ```
3. Натисніть **➕ Створити продукт**
4. **Перевірте консоль браузера** - ви повинні побачити:
   ```
   Form submitted: {name: "Тестовий продукт", ...}
   Sending data: {...}
   Create response: {message: "Продукт створено", productId: X}
   ```

## Крок 4: Тестування редагування

1. Знайдіть щойно створений продукт в таблиці
2. Натисніть **✏️ Редагувати**
3. Змініть значення (наприклад, прибутковість на 25%)
4. Натисніть **💾 Зберегти зміни**
5. **Перевірте консоль** - ви повинні побачити:
   ```
   Form submitted: {...}
   Editing product ID: X
   Sending data: {...}
   Update response: {message: "Продукт оновлено", changes: 1}
   ```

## Крок 5: Тестування деактивації

1. Натисніть кнопку **🚫 Деактивувати** біля продукту
2. **Перевірте консоль** - ви повинні побачити:
   ```
   Toggling product: X Current status: 1
   Sending update: {name: "...", is_active: false}
   Response: {message: "Продукт деактивовано", changes: 1}
   ```
3. Продукт повинен змінити статус на "✗ Неактивний"

## Крок 6: Тестування активації

1. Натисніть кнопку **✓ Активувати** біля деактивованого продукту
2. Продукт повинен змінити статус на "✓ Активний"

## 🐛 Що перевіряти в консолі при помилках:

### Якщо кнопки не працюють:

1. **Перевірте консоль браузера (F12)**
   - Червоні помилки вказують на проблеми
   - Зверніть увагу на статус код (400, 401, 500)

2. **Перевірте консоль сервера**
   - Backend логує всі запити
   - Шукайте рядки з "=== CREATE PRODUCT ===" або "=== UPDATE PRODUCT ==="

### Типові помилки:

**401 Unauthorized:**
```
❌ Проблема: Токен авторизації недійсний або відсутній
✅ Рішення: Вийдіть і увійдіть знову
```

**400 Bad Request:**
```
❌ Проблема: Неправильні дані (порожні поля, неправильний тип)
✅ Рішення: Перевірте всі обов'язкові поля
```

**500 Server Error:**
```
❌ Проблема: Помилка на сервері (БД, синтаксис)
✅ Рішення: Подивіться логи серверу в терміналі
```

**Network Error:**
```
❌ Проблема: Сервер не відповідає
✅ Рішення: Переконайтесь що backend запущений (http://localhost:5000)
```

## 📊 Тестування через Network вкладку

1. Відкрийте **F12 → Network**
2. Виконайте дію (створення/оновлення)
3. Знайдіть запит в списку (наприклад: `products` або `products/7`)
4. Клікніть на запит
5. Перегляньте:
   - **Headers** → Request URL, Method, Status Code
   - **Payload** → дані що відправили
   - **Response** → відповідь сервера

## 🧪 Тест через командний рядок

Якщо UI не працює, протестуйте API напряму:

```bash
cd backend
node test-products-api.js
```

**Перед запуском:**
1. Відкрийте `backend/test-products-api.js`
2. Замініть `'your_password_here'` на ваш реальний пароль
3. Запустіть скрипт

## ✅ Контрольний список

- [ ] Консоль браузера відкрита (F12)
- [ ] Сервери запущені (frontend + backend)
- [ ] Авторизація як admin пройшла
- [ ] Вкладка "Продукти" відкрита
- [ ] При створенні продукту з'являється success повідомлення
- [ ] При редагуванні зміни зберігаються
- [ ] При деактивації статус змінюється
- [ ] Консоль не показує червоних помилок
- [ ] Network показує статус 200/201 для запитів

## 🆘 Якщо нічого не працює:

1. **Перезапустіть сервери:**
   ```bash
   # Натисніть Ctrl+C в терміналі
   npm start
   ```

2. **Очистіть кеш браузера:**
   - Ctrl+Shift+Delete → Очистити кеш
   - Або Ctrl+F5 для hard refresh

3. **Перевірте базу даних:**
   ```bash
   cd backend
   node -e "const db = require('better-sqlite3')('database/investment.db'); console.log(db.prepare('SELECT * FROM investment_products').all());"
   ```

4. **Перевірте авторизацію:**
   ```javascript
   // В консолі браузера
   console.log(localStorage.getItem('token'));
   ```

## 📞 Звернення за допомогою

Якщо проблема залишається:
1. Скопіюйте повідомлення помилки з консолі браузера
2. Скопіюйте логи з консолі сервера
3. Опишіть які дії виконували

---

**Щасливого тестування! 🚀**
