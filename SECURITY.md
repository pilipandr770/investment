# Security Policy

## Supported Versions

| Version | Supported          |
| ------- | ------------------ |
| 1.0.x   | :white_check_mark: |

## Reporting a Vulnerability

Если вы обнаружили уязвимость в безопасности, пожалуйста:

**НЕ** создавайте публичный Issue.

Вместо этого:
1. Отправьте email на [указать email для security]
2. Опишите уязвимость детально
3. Укажите шаги для воспроизведения
4. Предложите возможное решение (если есть)

Мы ответим в течение 48 часов и будем работать над исправлением.

## Известные проблемы безопасности

- В demo режиме используется простой пароль для admin (`admin123`). **Обязательно смените его в production!**
- Убедитесь, что `.env` файлы не попадают в git
- Используйте HTTPS в production
- Регулярно обновляйте зависимости (`npm audit`)

## Рекомендации по безопасности

### Environment Variables
- Используйте сильные пароли (минимум 16 символов)
- JWT_SECRET должен быть минимум 32 символа
- Никогда не коммитьте `.env` файлы в git

### Production
- Используйте HTTPS (SSL сертификаты)
- Настройте Content Security Policy (CSP)
- Включите rate limiting
- Регулярные бэкапы базы данных
- Мониторинг логов на подозрительную активность

### Database
- Используйте prepared statements (уже реализовано)
- Регулярные бэкапы
- Ограничьте доступ к базе данных

### Dependencies
```bash
# Проверка уязвимостей
npm audit

# Автоматическое исправление
npm audit fix
```

Спасибо за помощь в обеспечении безопасности! 🔐
